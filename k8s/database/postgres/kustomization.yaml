apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- statefulset.yaml
- service.yaml

labels:
  - pairs:
    app: postgres
    part-of: synkro-platform
      component: database 
